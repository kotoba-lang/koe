(ns koe.booking-gate-conformance
  (:require [koe.booking-gate :as gate])
  (:export [main delegated]))

(defn delegated [action :i64 booking-present :i64 signed-present :i64] :i64
  (gate/delegated action booking-present signed-present))
(defn main [] :i64 42)
